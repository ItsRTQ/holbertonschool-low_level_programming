#include <stdio.h>
/**
 * main - starts the program
 * Description - counts from 0 to 9
 * Return: 0 if the fuction finished correctly
 */
int main(void)
{
	int ch;

	for (ch = 0;ch < 10; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}

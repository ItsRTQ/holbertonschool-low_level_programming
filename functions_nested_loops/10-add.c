#include "main.h"
/**
 * add - adds to numbers
 * @a: first parameter to be added
 * @b: second parameter to be added
 * Description - takes a, b as a parameter then adds them a+b
 * Return: it returns an int value thats the total of a+b
 */
int add(int a, int b)
{
	return (a + b);
}

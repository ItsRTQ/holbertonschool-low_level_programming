/**
* op_add - adds two numbers
* @a: recive a value
* @b: recive value
*
* Return: the addition of a and b
*/
int op_add(int a, int b)
{
	return (a + b);
}
/**
* op_sub - substracts two numbers
* @a: recive a value
* @b: recive value
*
* Return: the substraction of a & b
*/
int op_sub(int a, int b)
{
	return (a - b);
}
/**
* op_mul - multiply two numbers
* @a: recive a value
* @b: value recive
*
* Return: the product of a & b
*/
int op_mul(int a, int b)
{
	return (a * b);
}
/**
* op_div - divids two numbers
* @a: recive a value
* @b: recive value
*
* Return: the division of a & b
*/
int op_div(int a, int b)
{
	return (a / b);
}
/**
* op_mod - gets the module of two numbers
* @a: recive a value
* @b: value recive
*
* Return: the module of a & b
*/
int op_mod(int a, int b)
{
	return (a % b);
}
